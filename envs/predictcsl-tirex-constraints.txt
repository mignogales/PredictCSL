# Keep predictcsl-tirex on the intended TiRex2 stack.
#
# TiRex2 needs a newer torch than predictcsl-main and therefore stays isolated.
# Install GiftEval with --no-deps in this env: its declared numpy/matplotlib
# ranges conflict with TiRex2, but the project only needs its Python dataset
# loader here.
torch==2.8.0
numpy==2.1.3
pandas==2.2.3
matplotlib==3.9.4
# 2.17.1 (GIFT-Eval's historical pin) uses np.array(..., copy=False) in its
# Arrow formatter and crashes under TiRex2's required NumPy 2.x. 2.21.0 is the
# first release containing Hugging Face's NumPy-2 compatibility fix (#6976).
datasets==2.21.0
tirex-2==0.1.1
