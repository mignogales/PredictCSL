# Keep predictcsl-tirex on the intended TiRex2 stack.
#
# TiRex2 needs a newer torch than predictcsl-main and therefore stays isolated.
# The project uses the vanilla FlashRNN backend at runtime, so this env should
# prioritize stable imports over opportunistic resolver upgrades.
torch==2.8.0
numpy==2.1.3
pandas==2.2.3
matplotlib==3.9.4
tirex-2==0.1.1
