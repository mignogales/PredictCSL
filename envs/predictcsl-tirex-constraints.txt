# Keep predictcsl-tirex on the intended TiRex2 stack.
#
# TiRex2 needs a newer torch than predictcsl-main and therefore stays isolated.
# GiftEval pins the scientific stack to numpy 1.26 / matplotlib 3.9, which is
# also what the saved working TiRex snapshot used.
torch==2.8.0
numpy==1.26.4
pandas==2.2.3
matplotlib==3.9.4
tirex-2==0.1.1
