# Resource-constrained overlay for co-scheduling on a partially occupied GPU.
# It writes to the canonical long-lag output tree, so completed cells remain
# resumable by the normal master launcher.
extends: experiments_long_lag.yaml

batch_size: 4
dynamic_batching:
  enabled: true
  reference_context: 8192
  max_batch_size: 4
